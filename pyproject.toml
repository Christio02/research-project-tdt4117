[project]
name = "info-retr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christio02", email = "christopher.hoee@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
]

[project.scripts]
info-retr = "info_retr:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
